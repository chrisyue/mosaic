// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mosaic", "for mosaic support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mosaic", "enable mosaic support", "no");

if (PHP_MOSAIC != "no") {
	EXTENSION("mosaic", "mosaic.c");
}

